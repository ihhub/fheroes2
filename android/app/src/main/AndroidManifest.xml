<?xml version="1.0" encoding="utf-8"?>
<!-- Replace com.test.game with the identifier of your game below, e.g.
     com.gamemaker.game
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="auto">

    <uses-feature android:name="android.software.live_wallpaper" />

    <!-- OpenGL ES 2.0 -->
    <uses-feature android:glEsVersion="0x00020000" />

    <!-- Touchscreen support -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_label"
        android:supportsRtl="false">

        <service
            android:exported="true"
            android:name="org.fheroes2.SDLActivity"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>

            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/livewallpaper" />
        </service>

        <activity
            android:name="com.ipapps.homm2.livewallpaper.settings.MainActivity"
            android:parentActivityName="com.ipapps.homm2.livewallpaper.ToolsetActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_toolset"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">
        </activity>

        <activity
            android:name="com.ipapps.homm2.livewallpaper.ToolsetActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_toolset"
            android:label="@string/activity_toolset_label"
            android:launchMode="singleTask"
            android:taskAffinity="org.fheroes2.ToolsetTask"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

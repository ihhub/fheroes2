sudo: required

language: cpp

matrix:
  include:
    - os: linux
      before_install:
        - sudo apt-get update
        - sudo apt-get install -y libsdl-dev
        - sudo apt-get install -y libsdl-ttf
    - os: osx

script:
  - |
    make
